{
  "dependencies": {
    "aws-serverless-express": "^3.3.5",
    "express": "^4.16.4",
    "slackend": "^1.4.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.401.0",
    "dotenv": "^6.2.0"
  },
  "name": "slackbot",
  "private": true,
  "scripts": {
    "test": "docker-compose run --rm test"
  },
  "version": "12.4.0"
}
