{
  "dependencies": {
    "aws-serverless-express": "^3.3.5",
    "express": "^4.16.4",
    "slackend": "^2.3.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.427.0",
    "dotenv": "^7.0.0"
  },
  "name": "slackbot",
  "private": true,
  "scripts": {
    "test": "docker-compose run --rm test"
  },
  "version": "13.5.0"
}
