{
  "dependencies": {
    "aws-serverless-express": "^3.3.5",
    "express": "^4.16.4",
    "slackend": "^1.0.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.401.0"
  },
  "private": true,
  "scripts": {
    "package": "docker-compose run package && docker-compose down"
  }
}
